export default function EdgeUi({ message }: { message: string }) {
  return (
    <div className="my-6">
      <p className="text-gray-500 font-semibold font-lato text-center">{message}</p>
    </div>
  );
}
